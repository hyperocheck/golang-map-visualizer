<img width="959" height="540" alt="{5A8466E2-DBD6-4386-9576-3C5D7CA53884}" src="https://github.com/user-attachments/assets/15c7a4b9-affa-4c15-8433-4ff40055e793" />

## Golang old bucket map inspector
Это приложение для визуализации работы бакетной мапы в го. В файле src/usermap/usermap.go вам нужно задать тип возвращаемой мапы в функции `GetUserMap` и вернуть ее, например:
```go
/*  ⚠️напиши тип мапы, которая возвращается ⚠️ */
func GetUserMap() map[int]bool /* <-- ⚠️ */ {
	m := map[int]bool{}
	
	// тут можешь заранее сделать любые действия с мапой перед тем, как пользоваться консольным вводом
	// например заполнить ее случайными значениями, переудалять все или обновить
	// если у тебя в качестве ключа или значения выступаем составной тип, то необходимо имплементировать
	// его парсер/конструктор в src/console, там есть пример для ComplexStructExample
	for i := range 100 {
      if i%2 == 0 {
        m[i] = false
      } else {
        m[i] = true
      }
	}
	return m
}
```
😲*ВЕРСИЯ ГО СТРОГО 1.23 ИЛИ НИЖЕ, Т.К. ПОСЛЕ ЭТОЙ ВЕРСИИ УСТРОЙСТВО МАПЫ ПОЛНОСТЬЮ ИЗМЕНИЛОСЬ*😲
----
Далее необходимо из коревой папки проекта запустить программу `go1.23 run ./cmd/visualizer`.
На `http://localhost:8080` будет доступна визуализация. На данный момент обовлять страницу, чтобы увидеть изменения в визуализации, нужную вручную [TODO websocket]
